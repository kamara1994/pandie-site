import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Get Involved",
  description: "Donate, sponsor a child, volunteer your skills, or partner with Pandie Foundation — every way to stand with Sierra Leone's children.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
